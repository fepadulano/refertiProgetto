export const environment = {
  apiUrl: "http://localhost:3000/api",
  // Chiave di test ufficiale di Google reCAPTCHA v2 (valida sempre, per
  // localhost). In produzione va sostituita con una site key vera.
  recaptchaSiteKey: "6LeIxAcTAAAAAJcZVRqyHh71UMIEGNQ_MXjiZKhI",
  // Base del server (senza /api): è quella a cui si collega Socket.io.
  socketUrl: "http://localhost:3000",
};

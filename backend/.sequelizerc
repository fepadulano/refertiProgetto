const path = require("path");

module.exports = {
  config: path.resolve("src/frameworks/database/config", "config.js"),
  "migrations-path": path.resolve("src/frameworks/database", "migrations"),
  "seeders-path": path.resolve("src/frameworks/database", "seeders"),
  "models-path": path.resolve("src/frameworks/database", "models"),
};

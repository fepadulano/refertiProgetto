DB_NAME=nome_db_medico
DB_USER=postgres
DB_PASSWORD=changeme
DB_HOST=localhost

JWT_SECRET=changeme
JWT_EXPIRES_IN=2h

PORT=3000

UPLOAD_DIR=uploads/referti
# Chiave AES-256 (32 byte) in esadecimale, usata per cifrare i referti a riposo.
# Generarne una nuova con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
FILE_ENCRYPTION_KEY=changeme

# Chiave di test ufficiale di Google reCAPTCHA v2 (valida sempre, per localhost).
# In produzione va sostituita con la secret key vera, generata su
# https://www.google.com/recaptcha/admin per il proprio dominio.
RECAPTCHA_SECRET_KEY=6LeIxAcTAAAAAGG-vFI1TnRWxMZNFuojJ4WifJWe

CORS_ORIGIN=http://localhost:4200
